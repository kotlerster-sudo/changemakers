[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "changemakers"
version = "0.0.1"
description = "Mobile API for Changemakers"
authors = [
    {name = "Arunodhaya"}
]

[tool.flit.module]
name = "changemakers"
# force update
